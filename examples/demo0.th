data nat contains
    one 
    succ nat

data bool contains
    true
    false

define main of_type bool as is_one succ

define is_one of_type fn nat bool as lambda x match x with
    one to true
    _   to false

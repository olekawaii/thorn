include tuple bool

type ordering contains
    lt
    gt
    eq

define eq_ordering as the fn ordering fn ordering bool
    lambda x lambda y match tuple x y
        case tuple gt gt        true
        case tuple lt lt        true
        case tuple eq eq        true
        case _                  false
